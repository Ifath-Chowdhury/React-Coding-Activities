import React from 'react';

const ProfileCard = ({ image, name, jobTitle, bio }) => {
    return (
    <div className="profile-card">
        <div className="left-column">
            
        </div>
        <div className="right-column">
            /* jobTitle and bio go here*/
        </div>
    </div>
    );
  };
  
export default ProfileCard;